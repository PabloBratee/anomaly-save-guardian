name: CI

on:
  push:
  pull_request:

jobs:
  syntax:
    name: PowerShell syntax check
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Parse PowerShell scripts
        shell: powershell
        run: .\scripts\check-syntax.ps1
